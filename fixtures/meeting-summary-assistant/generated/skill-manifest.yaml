fixtureId: meeting-summary-assistant
fixtureVersion: 0.1.0
kind: skill-manifest
spec:
  kind: skill-spec
  path: ../skill-spec.yaml
  fixtureId: meeting-summary-assistant
  fixtureVersion: 0.1.0
skills:
  - id: meeting-summary-assistant
    name: meeting-summary-assistant
    version: 0.1.0
    title: 会议纪要整理助手
    entry: generated/skill/SKILL.md
    description: 输入公开或虚构会议记录，输出结构化中文会议纪要。
    tags:
      - meeting
      - summary
      - zh-CN
    permissions:
      network: false
      externalSend: false
      fileWrite: false
      destructiveOperations: false
      privatePathRead: false
    dependencies:
      - none
    status: static_checked
checklist:
  structure: manifest 包含技能 ID、版本、标题、入口、权限、依赖和状态。
  trigger: manifest 描述会议纪要整理主题。
  boundary: 权限默认保守，状态仅表示静态检查通过。
  dependency: 不声明外部依赖。
  replay: 与 replay-cases.yaml 使用同一 fixtureId。
  privacy: 仅处理公开或虚构输入。
  compatibility: entry 使用相对路径 generated/skill/SKILL.md，且对应样本文件存在。
