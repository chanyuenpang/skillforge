fixtureId: task-planning
fixtureVersion: 0.1.0
kind: generation-run
run:
  mode: static
  source: <SKILL_SOURCE_PATH>
  generator: skillforge-fixture-writer
  assumptions:
    - 只做最小边界映射，不实现调度引擎
    - 保留 leader-planning 语义
  outputs:
    - README.md
    - workflow-source.yaml
    - skill-spec.yaml
    - skill-manifest.yaml
    - replay-cases.yaml
    - validation-result.yaml
    - skill/SKILL.md
  status: generated
