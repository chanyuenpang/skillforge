fixtureId: browser-agent-workflow
fixtureVersion: 0.1.0
kind: generation-run
run:
  mode: static
  source: skills/browser-agent-workflow/SKILL.md
  generator: skillforge-fixture-writer
  assumptions:
    - 只做最小边界映射，不实现真实浏览器编排
    - 保留脚本优先与 BrowserOS MCP 隔离语义
  outputs:
    - README.md
    - workflow-source.yaml
    - skill-spec.yaml
    - skill-manifest.yaml
    - replay-cases.yaml
    - validation-result.yaml
    - skill/SKILL.md
  status: generated
